<body class="h-screen bg-gradient-to-b from-yellow-100 to-blue-400 overscroll-none">
  

  <div class = "mx-auto text-center">
    <header class = "bg-blue-400 font-bold p-3 mx-auto">
     <div class= "p-2">
        <ul class = "list-none text-center mx-auto">
          <h1 class = "text-4xl no-underline text-green-400"><%= link_to "KɪᴄᴋᴇᴅOғғEʙᴀʏ🏠", '/'%></h1>
          <h2 class = "mx-auto text-2xl">Because ➡️➡️➡️➡️ Everyone🫵 Deserves💪 a Second♻️ Chance😇</h2>
              <% if logged_in? %>
          <h3 class = "text 2xl">Hello! <%=current_user.name %></h3><br>
            <a href="/home">  
            <button>My profile</button></a>
            <a href="/logout">  
            <button>Logout</button></a> 
          <% else %>
            <a href="/login">  
            <button class= "text-2xl rounded-none hover:rounded-full">Login</button></a>
            <a href="/users/new">  
            <button class= "text-2xl rounded-none hover:rounded-full">Sign Up</button></a> 
          <% end %>
            <h3 class = "mx-auto text-2xl">🔎🅢🅔🅐🅡🅒🅗🔎</h3>
            <%= form_tag(products_path, method: :get) do %>
            <%= text_field_tag(:search, params[:search])%>
            <%end%>
        </ul>
      </div>
    </header>

    <div>
      <h1 class = "text-center text-6xl font-bold text-blue-400"><%=@user.name%></h1>
    </div>

    <div>
      <h2 class = "text-5xl font-semibold text-blue-400">
        <%=@user.category_1%> 
        <%=@user.category_2%> 
        <%=@user.category_3%>
      </h2>
    </div>

    

    <div>
      <h2 class = "text-4xl text-blue-500 underline">Products for $ale</h2>
      <ul class = "list-none text-3xl"><%Product.all.filter { |p| p.user_id == @user.id }.each do |prod| %> <%#User.products not working %> 
        <li>🤑 <%=link_to prod.name, prod%></li>
        <%end%>
      </ul>
    </div>

    <div class = "text-2xl">
      <%= link_to "See All Users", users_path %>
      <% if @user.id == current_user.id%>
        | <%= link_to "Edit this user", edit_user_path(@user) %> | 
        <%= link_to "Sell Your Stuff!", '/products/new'%>
        <%= button_to "Delete your profile", @user, method: :delete %>
      <% end %>
    </div>

  </div>

</body>


