<body class="h-screen bg-gradient-to-b from-yellow-100 to-blue-400 overscroll-none text-center"> 

  <di class = "mx-auto text-center">
    <h1 class= "font-bold text-6xl text-center text-blue-400">👋, <%=current_user.name%>!</h1>
    <h2  class= "font-bold text-5xl text-center text-blue-400">Let's Edit Your Profile</h1>

    <div>
      <%= form_for @user do |f| %>
        <div class = "">
          <%= f.label :name, style: "display: block" %>
          <%= f.text_field :name %>
        </div>

      <h3> Please Select Your Interests </h3>
      <div>
        <%= f.label :interest, style: "display: block" %>
        <%= f.collection_select(:category_ids, Category.all, :id, :name) %>
      </div>

      <div>
        <%= f.label :interest, style: "display: block" %>
        <%= f.collection_select(:category_ids, Category.all, :id, :name) %>
      </div>

      <div>
        <%= f.label :interest, style: "display: block" %>
        <%= f.collection_select(:category_ids, Category.all, :id, :name) %>
      </div><br>

       <a href="/"><button class = "text-4xl bg-green-400 rounded-full">Home Page</button></a><br><br>
     

      <div>
        <%= f.submit "Update Your Profile!"%>
      </div>
      <%end%></p>

       <input class = "text-3xl rounded-full bg-rose-400 hover:bg-rose-800"type=submit value="Logout"><br><br>
    </div>
   </div>
</body>
<br>

<div>
  <%= link_to "#{@user.name}", @user %> |
  <%= link_to "Back to users", users_path %>
</div>
