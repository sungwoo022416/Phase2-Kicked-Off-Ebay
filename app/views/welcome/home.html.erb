<body class="h-screen bg-gradient-to-b from-yellow-100 to-blue-400 overscroll-none text-center"> 

  <div>
    <% if logged_in? %>
        <h1>You are Logged In, <%= current_user.name %></h1>
    <%end%>
    <%= form_tag ({controller: 'sessions', action: 'destroy', method: 'post'}) do %>
    <input type=submit value='logout'>
    <a href="/">  
    <button>Home Page</button></a>
     <a href="/users">  
    <button>Seller List</button></a>
      <a href="/bids">  
    <button>Bids List</button></a>
 
    <% end %>
    </div>
</body>